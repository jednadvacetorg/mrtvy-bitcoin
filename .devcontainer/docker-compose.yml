services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ..:/workspace
    environment:
      - OPENCODE_CONFIG=/workspace/.devcontainer/opencode-config.json
      - XDG_DATA_HOME=/workspace/.devcontainer/data
    command: sleep infinity
